\section{Exercise 6} \label{P5}
\subsection{Q6.1}
$^0_1 T = \begin{pmatrix}
        ? & ? & ? & ? \\
        ? & ? & ? & ? \\
        ? & ? & ? & ? \\
        ? & ? & ? & ?
    \end{pmatrix}$
    
\textit{[Comment] Same structure for the other matrices } 
